services:
  onboarding-web-prod:
    container_name: onboarding-web-prod
    build:
      context: .
      target: production
      args:
        VITE_API_BASE_URL: ${PROD_VITE_API_BASE_URL:-https://api.seu-dominio.com}
    ports:
      - "${PROD_FRONTEND_PORT:-80}:80"
    restart: always
