services:
  onboarding-web-prod:
    container_name: onboarding-web-prod
    build:
      context: .
      target: production
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    env_file:
      - ./docker/env/prod.env
    ports:
      - "${FRONTEND_PORT:-80}:80"
    restart: always
